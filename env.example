# Copy this file to .env (local only) and fill the values. Never commit .env with real secrets.
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: other env vars used by the app
# VITE_OTHER_VAR=
