// Theme context barrel that only re-exports the ThemeProvider component.
// Avoid exporting hooks or non-component values from this module to keep
// compatibility with React Fast Refresh.
export { ThemeProvider } from "./theme-provider.tsx";
